cd workshop
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m ipykernel install --user --name=workshop --display-name "Workshop (venv)"
ollama pull llama3.2
ollama pull nomic-embed-text